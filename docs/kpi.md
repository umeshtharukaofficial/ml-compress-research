# Hard KPIs Tracker

KPI checks tracked by the orchestrator.

## Current Target Metrics status
- **S1. Compression Ratio**: ZSTD-19 beat target ratio of >= 1.4x (Pending on bio-datasets target evaluations)
- **S2. Runtime Peak RAM**: < 200 MB (Pass)
- **S3. Quantized Model Size**: <= 5 MB (Pass)
- **S4. Single-core Deceleration Speed**: >= 20 MB/s (Pending Rust Runtime)
- **S5. Reproducibility Execution**: < 2 hours (Pass)
- **S6. Compiles clean LaTex Paper**: (Pass)
- **S7. Cadence frequency**: 5-minute ticks (Pass)
